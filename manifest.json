{
  "name": "SearchBarAutoFocus",
  "version": "1.0",
  "description": "One extension that enables autofocus on search bars for many sites which don't have it naturally",
  "permissions": ["activeTab"],
  "content_scripts": [{
    "matches": ["*://*/*"],
    "js": [
      "enabledSites.js",
      "main.js"
    ]
  }],
  "manifest_version": 2
}
