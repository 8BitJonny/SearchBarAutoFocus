{
  "name": "SearchBar Auto Focus",
  "version": "1.0",
  "description": "An extension that enables auto focusing search bars upon website loading for many sites which don't have it naturally.",
  "permissions": ["activeTab"],
  "content_scripts": [{
    "matches": ["*://*/*"],
    "js": [
      "enabledSites.js",
      "main.js"
    ]
  }],
  "manifest_version": 2
}
